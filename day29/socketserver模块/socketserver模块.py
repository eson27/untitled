#socket 底层模块
#socketserver 基于socket完成的
#处理tcp 并发 以后写tcp尽量用socketserver

# import socket
# sk = socket.socket()
# sk.bind(('170,0.0.1',9001))
# sk.listen()
#
#
# conn,_=sk.accept()
# while True
#     content = conn.recv(1024).decode('utf-8')
#import time
from time import sleep
sleep(1)
print("1")
